# Base URL for the ASP.NET Core API — matches the "http" launch profile's
# default port (SukotSystemApi/Properties/launchSettings.json). Update if the
# server runs on a different port on your machine.
VITE_API_BASE_URL=http://localhost:5114/api
